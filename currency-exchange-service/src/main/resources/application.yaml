server:
  port: 8000
spring:
  application:
    name: currency-exchange
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
eureka:
  client:
    enabled: false